{
  "name": "child-health-tracker-backend",
  "version": "1.0.0",
  "description": "Node.js Express backend for Child Health & Allergy Tracker",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build": "npm install --prefix ../frontend && cross-env CI=false npm run build --prefix ../frontend",
    "postinstall": "npm install --prefix ../frontend && cross-env CI=false npm run build --prefix ../frontend"
  },
  "dependencies": {
    "@google/generative-ai": "^0.11.0",
    "cors": "^2.8.5",
    "cross-env": "^7.0.3",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "pg": "^8.11.3"
  }
}
